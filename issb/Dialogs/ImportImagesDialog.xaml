<Window x:Class="issb.ImportImagesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Происходит импортирование..." Height="300" Width="300" Loaded="Window_Loaded"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>
    </Window.Resources>
    <StackPanel>
        <ListBox Name="ProcessedFilesListBox" Height="250" Width="400">

        </ListBox>
        <Border>
            <Label Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center" Content="Происходит импортирование выбранных файлов..."/>
        </Border>
    </StackPanel>
</Window>
